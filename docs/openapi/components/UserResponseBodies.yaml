components:
  schemas:
    UserSearchResponse:
      type: object
      properties:
        uid:
          type: integer
          format: int64
          description: "유저 ID"
        nickname:
          type: string
          description: "유저 닉네임"