post:
  summary: "로그인 성공 시 accessToken(1h) + refreshToken(7d) 발급."
  tags:
    - Auth
  responses:
    '200':
      description: "로그인 성공"
      content:
        application/json:
          schema:
            $ref: "../components/User.yaml"
    '401':
      description: "이메일 혹은 pw 틀림"